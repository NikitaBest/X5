import { CryptlexTokenData, HttpRequest } from '@biosensesignal/wasm';
import { LicenseOptions } from './license.types';
export declare class LicenseManager {
    private licenseData;
    private onEnabledVitalSigns;
    private onOfflineMeasurement;
    private onActivation;
    private onLicenseError;
    init({ licenseInfo, onLicenseError, }: LicenseOptions): Promise<void>;
    private setLicenseMessageListener;
    private setLicenseHttpRequestListener;
    sendInfoMessages(licenseData: CryptlexTokenData): void;
    calculateSecondsToEnd(licenseData: any): number;
    handleHttpRequest(request: HttpRequest): Promise<void>;
    private handleHttpResponse;
    private handleTimeoutHttpResponse;
    private handleErrorHttpResponse;
}
