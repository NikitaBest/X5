import { FrameData } from '../common/types';
import { DeviceOrientation } from '../index';
import { HealthMonitorProcessor, ProcessorOptions } from './processor.types';
export declare class ProcessorManager implements HealthMonitorProcessor {
    private readonly SDK_PROCESSOR_START_REQUEST_NOT_FINISHED_ERROR;
    private readonly SDK_PROCESSOR_STOP_REQUEST_NOT_FINISHED_ERROR;
    private readonly SDK_PROCESSOR_STOP_REQUEST_WAITING_FOR_NON_FINISHED_START_REQUEST_ERROR;
    private readonly state;
    private readonly onFaceDetected;
    private readonly onImageData;
    private readonly onVitalSign;
    private readonly onFinalResults;
    private readonly onError;
    private readonly onWarning?;
    private readonly resolveFeatures;
    private readonly logAlgoMeasurements;
    private readonly coreLogging;
    private readonly doRecording;
    private readonly doMeasuring;
    private readonly precomputeReportData;
    private readonly vitalSignsLogger;
    private readonly rriLogger;
    private osType;
    private readonly logs;
    private measurementStartTime;
    private measurementDurationInSec;
    private errorListener;
    private userInformation;
    private orientation;
    private strictMeasurementGuidance;
    private lastImageWidth;
    private lastImageHeight;
    private readonly cameraId;
    private readonly licenseOptions;
    private readonly encryptLogs;
    private processingTime;
    private measurementError;
    private finalResults;
    private startFinished;
    private stopFinished;
    private terminateFinished;
    private pendingTerminate;
    private bloodPressureEnabled;
    private bloodTestEnabled;
    private static isFirstTime;
    private measurementId;
    constructor({ onFaceDetected, onImageData, onVitalSign, onFinalResults, onError, onWarning, resolveFeatures, logAlgoMeasurements, coreLogging, doRecording, doMeasuring, precomputeReportData, vitalSignsLogger, rriLogger, requestedOrientation, userInformation, strictMeasurementGuidance, cameraId, licenseOptions, encryptLogs, processingTime, bloodPressureEnabled, bloodTestEnabled, }: ProcessorOptions);
    init(): Promise<void>;
    private setReportListener;
    private setFaceListener;
    private setImageDataListener;
    setErrorListener: (onError: any) => void;
    private setMessageListener;
    private publishError;
    private publishWarning;
    private publishRefreshWarningIfNeeded;
    private publishAlgoMessage;
    private hasFrameDimensionsChanged;
    private initEngine;
    start(): Promise<void>;
    process(frameData: FrameData, deviceOrientation: DeviceOrientation, invalidOrientation: boolean): void;
    stop(withReport: boolean): Promise<void>;
    close(): Promise<void>;
    private blockUntilProcessing;
    private blockUntilStartFinish;
    private blockUntilStopFinish;
    private blockUntilpendingTerminateFinish;
    private getMeasurementTime;
    private publishLogs;
    private resolveMeasurementId;
}
