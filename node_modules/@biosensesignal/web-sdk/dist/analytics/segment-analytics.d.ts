import { AnalyticsApi } from "./analytics-api";
import { SDKAnalyticEvent } from "./sdk-analytic-event";
export declare class SegmentAnalytics implements AnalyticsApi {
    private analytics;
    private extraData;
    init(segmentWriteKey: string, sessionId: string): void;
    sendEvent(event: SDKAnalyticEvent): void;
}
