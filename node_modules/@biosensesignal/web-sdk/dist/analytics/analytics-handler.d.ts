import { EnabledVitalSigns } from "../license/license.types";
import { AlertData, VitalSignsResults } from "../common/types";
import { DeviceOrientation } from "../device/types";
import { UserInformation } from "../session/session.types";
import { AnalyticsApi } from "./analytics-api";
export declare class AnalyticsHandler {
    private static instance;
    private static analyticsApi;
    private static licenseActivationId?;
    private static measurementRequestedDuration?;
    private static enabledVitalSigns?;
    static getInstance(): AnalyticsHandler;
    setAnalyticsApi(analyticsApi: AnalyticsApi): void;
    setLicenseActivationId(licenseActivationId: string): void;
    setEnabledVitalSigns(enabledVitalSigns: EnabledVitalSigns): void;
    handleWarning(message: AlertData): void;
    handleError(message: AlertData): void;
    handleMeasurementStarted(measurementStartTime: number, measurementRequestedDuration: number): void;
    handleSessionTerminated(): void;
    handleFinalResults(measurementDuration: number, finalResults?: VitalSignsResults, error?: number): void;
    handleSessionCreated(strictMeasurementGuidance: boolean, vitalSignListener: boolean, imageListener: boolean, userInformation?: UserInformation, deviceOrientaion?: DeviceOrientation): void;
    private static isVitalSignEnabled;
}
